import JoshuKit

/// Registry entry point for the built-in widget types.
/// Populated as widgets land: Coding (M6), Reviewer (M7), Meeting (M8b).
public enum BuiltinWidgets {
    // M1 introduces AnyWidgetDescriptor; until then this is an empty anchor
    // that keeps the package building and the app linking against it.
}
